---
name: llm-architecture-analyzer
description: 大模型架构解析工具。当用户要求解析、拆解、讲解某个大语言模型/多模态模型（如 GPT、Llama、Qwen、DeepSeek、Kimi、Grok、Gemini、Claude、Mixtral、MoE 模型等）的架构、结构组成、模块设计，或询问某模型"每个节点是什么/做什么/为什么这样设计"时使用。输出逐节点的五维解析（是什么、内容是什么、做什么、目标、意义），并在信息含糊时执行反向 grill-me 自我拷问补全证据，条件允许时附架构流程图。
---

# 大模型架构解析工具

将目标大模型拆解为可枚举的节点（组件/模块），逐节点按五维框架解析，并用反向 grill-me 方法消除不确定性，最后生成架构流程图（如具备生图/图表能力）。

## 工作流程

### 1. 确认目标与收集权威资料

- 明确用户要解析的模型（名称、版本、是否某篇论文/某次发布的变体）。
- 检索权威来源：官方论文（arXiv）、官方技术报告、官方博客、官方 GitHub 仓库（config.json、源码）、HuggingFace 模型卡。时间敏感信息必须搜索验证，不得凭记忆编造参数量、层数、维度等硬数字。
- 硬数字（层数、hidden size、注意力头数、词表大小、上下文长度、激活参数量等）逐条标注来源；查不到的标注"未公开"而非猜测。

### 2. 拆解节点

按"整体 → 宏模块 → 子模块 → 关键组件"分层拆解，确保无遗漏、无重叠。参考 [references/node_taxonomy.md](references/node_taxonomy.md) 中的标准节点清单逐项核对目标模型（有什么写什么，没有的明确说明无该组件）。

典型分层：
1. 整体架构（如 decoder-only Transformer、encoder-decoder、MoE）
2. 输入侧（分词器、嵌入层、位置编码）
3. 核心块（注意力机制、前馈网络/MoE、归一化、残差连接、激活函数）
4. 输出侧（输出头、采样/解码策略）
5. 训练与对齐（若用户要求：预训练目标、数据、RLHF/DPO、基础设施）
6. 推理优化（若适用：KV cache、量化、投机解码等）

### 3. 逐节点五维解析

每个节点必须分条回答以下五个问题（缺一条即不完整）：

1. **是什么**——该节点的身份定义与在整体中的位置。
2. **内容是什么**——内部构成：数学形式、超参数、具体配置（带数字与来源）。
3. **做什么**——功能机制：输入什么、输出什么、如何变换。
4. **目标是什么**——设计意图：为了解决什么问题而引入。
5. **意义是什么**——影响与价值：对性能/效率/能力的贡献，对比替代方案的取舍。

输出格式模板见 [references/output_template.md](references/output_template.md)。

### 4. 反向 grill-me 自我拷问

当任何节点的内容含糊、数字不确定、来源冲突、或自己只能用"可能/大概/通常"表述时，**不要含糊带过**——执行反向 grill-me：以拷问者的姿态对自己发起连续追问，直到每个问题都有答案或明确标注"无法确认"。

拷打流程：
1. 列出该节点所有含糊点，逐条转成尖锐的问题（"层数到底是 32 还是 36？依据是什么？"）。
2. 对每个问题：先凭已有证据回答 → 证据不足则搜索/查论文/查官方 config → 仍无答案则明确写"公开资料未披露"，并说明已尝试的验证路径。
3. 禁止自欺欺人：每个结论必须能指出证据来源；把"常识性推断"与"已证实事实"分开标注。
4. 拷问结束后回看全部节点，检查是否有节点间的矛盾（如各模块维度对不上），发现矛盾回到第 2 步。

拷问问题清单模板见 [references/grill_me_checklist.md](references/grill_me_checklist.md)。

### 5. 生成架构流程图（条件触发）

如当前环境具备生图或图表能力（图像生成插件、mermaid/绘图代码等），生成一张架构流程图作为附件：

- 内容：数据流向为主线（输入 → 嵌入 → N 层 Transformer 块 → 输出头），每个节点标注关键配置（如"GQA, 8 KV heads"）。
- 优先使用可编辑/代码化图表（mermaid、graphviz、绘图代码渲染 PNG）；无此类能力时才用 AI 生图，且生图后须检查文字标注是否正确。
- 图中节点与正文解析的节点一一对应，不出现正文没有的组件。

### 6. 汇总交付

- 正文：整体概述（一段话）→ 逐节点五维解析 → 不确定项清单（含"未公开"标注与验证尝试）。
- 附件：架构流程图（如已生成）。
- 语言：使用用户的语言；术语首次出现给出中英文对照。

## 注意事项

- 硬数字必须带来源；无法验证即标注"未公开"，绝不编造。
- 同名模型有多个版本时（如 Llama 2 vs 3），先确认版本再解析；版本间架构差异要指出。
- 闭源模型（如 GPT-4）只解析官方已披露或论文可考的部分，其余归入"未公开"清单。
